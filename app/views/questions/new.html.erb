<%= form_for(@question) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>

  <%= f.label :description %>
  <%= f.text_area :description %><br>

  <%= f.hidden_field(:user_id, :value => session[:user_id]) %>

  <%= f.submit "create new question" %>
<% end %>